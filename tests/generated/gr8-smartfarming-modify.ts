// AUTO-GENERATED by md-to-ts.py -- do not edit.
// Source: curriculum/gr8-smartfarming-modify.md

// gr8-smartfarming-modify.md :: ```template fence #1
namespace gr8_smartfarming_modify_template01 {
let pumpStop = 0
let pumpStart = 0
input.onButtonPressed(Button.A, function () {
    pumpStart = input.runningTime()
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
    }
    pumpStop = input.runningTime()
})
input.onButtonPressed(Button.B, function () {
    basic.showLeds(`
        # # # # #
        # . # . #
        # . # # #
        # . . . #
        # # # # #
        `)
    basic.clearScreen()
    basic.showNumber(Math.round((pumpStop - pumpStart) / 1000))
    basic.pause(2000)
    basic.clearScreen()
})
}

// gr8-smartfarming-modify.md :: ```block fence #1
namespace gr8_smartfarming_modify_block01 {
let pumpStart = 0
let pumpStop = 0
while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        // @highlight
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
    }
}

// gr8-smartfarming-modify.md :: ```blocks fence #1
namespace gr8_smartfarming_modify_blocks01 {
let pumpStart = 0
let pumpStop = 0
input.onButtonPressed(Button.A, function () {
    // @highlight
    pumpStart = input.runningTime()
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
    }
    // @highlight
    pumpStop = input.runningTime()
})
input.onButtonPressed(Button.B, function () {
    basic.showLeds(`
        # # # # #
        # . # . #
        # . # # #
        # . . . #
        # # # # #
        `)
    basic.clearScreen()
    // @highlight
    basic.showNumber(Math.round((pumpStop - pumpStart) / 1000))
    basic.pause(2000)
    basic.clearScreen()
})
}

// gr8-smartfarming-modify.md :: ```blocks fence #2
namespace gr8_smartfarming_modify_blocks02 {
let pumpStart = 0
let pumpStop = 0
// @highlight
input.onButtonPressed(Button.A, function () {
    pumpStart = input.runningTime()
    // @highlight
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
    }
    pumpStop = input.runningTime()
})
}

// gr8-smartfarming-modify.md :: ```blocks fence #3
namespace gr8_smartfarming_modify_blocks03 {
let pumpStop = 0
let pumpStart = 0
// @highlight
basic.forever(function () {
    pumpStart = input.runningTime()
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
    }
    pumpStop = input.runningTime()
})
}

// gr8-smartfarming-modify.md :: ```blocks fence #4
namespace gr8_smartfarming_modify_blocks04 {
let pumpStop = 0
let pumpStart = 0
// @highlight
loops.everyInterval(120000, function () {
    pumpStart = input.runningTime()
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
    }
    pumpStop = input.runningTime()
})
}

// gr8-smartfarming-modify.md :: ```blocks fence #5
namespace gr8_smartfarming_modify_blocks05 {
let pumpStop = 0
let pumpStart = 0
loops.everyInterval(120000, function () {
    // @highlight
    music.play(music.tonePlayable(262, music.beat(BeatFraction.Whole)), music.PlaybackMode.UntilDone)
    pumpStart = input.runningTime()
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
    }
    pumpStop = input.runningTime()
})
}

// gr8-smartfarming-modify.md :: ```blocks fence #6
namespace gr8_smartfarming_modify_blocks06 {
let pumpStop = 0
let pumpStart = 0
loops.everyInterval(120000, function () {
    music.play(music.tonePlayable(262, music.beat(BeatFraction.Whole)), music.PlaybackMode.UntilDone)
    // @highlight
    fwdLights.ledRing1.setPixelColor(0, 0xff0000)
    pumpStart = input.runningTime()
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
        // @highlight
        fwdLights.ledRing1.rotate(1)
    }
    pumpStop = input.runningTime()
})
}

// gr8-smartfarming-modify.md :: ```blocks fence #7
namespace gr8_smartfarming_modify_blocks07 {
let pumpStop = 0
let pumpStart = 0
loops.everyInterval(120000, function () {
    music.play(music.tonePlayable(262, music.beat(BeatFraction.Whole)), music.PlaybackMode.UntilDone)
    fwdLights.ledRing1.setPixelColor(0, 0xff0000)
    pumpStart = input.runningTime()
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
        fwdLights.ledRing1.rotate(1)
    }
    pumpStop = input.runningTime()
    // @highlight
    fwdLights.ledRing1.setAllPixelsColor(0x00ff00)
})
}

// gr8-smartfarming-modify.md :: ```blocks fence #8
namespace gr8_smartfarming_modify_blocks08 {
let pumpStop = 0
let pumpStart = 0
loops.everyInterval(120000, function () {
    music.play(music.tonePlayable(262, music.beat(BeatFraction.Whole)), music.PlaybackMode.UntilDone)
    // @highlight
    fwdLights.ledRing1.setAllPixelsColor(0x000000)
    fwdLights.ledRing1.setPixelColor(0, 0xff0000)
    pumpStart = input.runningTime()
    // @collapsed
    while (fwdSensors.moisture1.isPastThreshold(20, fwdEnums.OverUnder.Under)) {
        fwdMotors.pump.timedRun(500)
        basic.pause(1000)
        fwdLights.ledRing1.rotate(1)
    }
    pumpStop = input.runningTime()
    fwdLights.ledRing1.setAllPixelsColor(0x00ff00)
})
}
